# دليل الاختبار

## ✅ الاختبارات المطلوبة

### 1. اختبار RTL/LTR للـ Dropdowns

#### الخطوات:
1. اذهب إلى `/dashboard`
2. اضغط على أيقونة اللغة (في الـ navbar)
3. اختر "العربية"
4. اضغط على أيقونة البروفايل (يمين الـ navbar)
5. **تحقق**: الـ dropdown يجب أن يظهر على اليسار

#### التكرار بالإنجليزية:
1. اضغط على أيقونة اللغة
2. اختر "English"
3. اضغط على أيقونة البروفايل
4. **تحقق**: الـ dropdown يجب أن يظهر على اليمين

#### النتيجة المتوقعة:
- ✅ الـ dropdown يتغير موضعه حسب اللغة
- ✅ لا يكون هناك overlapping مع العناصر الأخرى

---

### 2. اختبار Bot Context - تبديل الـ Bot

#### الخطوات:
1. اذهب إلى `/dashboard`
2. افتح الـ sidebar (اضغط على Menu في mobile)
3. اضغط على "Bot 1" (الـ active bot)
4. اختر "Bot 2" من القائمة
5. **تحقق**: Dashboard يجب أن يتحدث فوراً

#### الملاحظات المتوقعة:
- ✅ اسم الـ Bot يتغير
- ✅ الوصف يتغير
- ✅ الإحصائيات تتغير (message count, channels, etc)
- ✅ لا توجد refresh للصفحة

---

### 3. اختبار Knowledge Page مع Bot Context

#### الخطوات:
1. اذهب إلى `/dashboard`
2. اختر "Bot 1" من الـ sidebar
3. اضغط على "Knowledge & Instructions" من الـ navbar الثانوي
4. **تحقق**: تظهر المعارف الخاصة بـ Bot 1

#### التكرار:
1. عُد للـ sidebar
2. اختر "Bot 2"
3. اضغط على "Knowledge & Instructions"
4. **تحقق**: تظهر المعارف الخاصة بـ Bot 2 (وهي مختلفة)

---

### 4. اختبار Responsive على Desktop

#### الخطوات:
1. فتح DevTools (F12)
2. اختر Desktop view (بعرض كامل)
3. تحقق من الـ sidebar يظهر على اليسار في العربية
4. تبديل إلى اللغة الإنجليزية
5. **تحقق**: الـ sidebar يجب أن يبقى مرئياً (لا يختفي)

---

### 5. اختبار تبديل اللغة مع البيانات

#### الخطوات:
1. اذهب إلى `/dashboard`
2. اختر "Bot 1"
3. اضغط على أيقونة اللغة
4. اختر "English"
5. **بعد الـ reload**:
   - ✅ يجب أن يكون Bot 1 لا يزال مختاراً
   - ✅ Dashboard يعرض البيانات الصحيحة

---

## 🧪 اختبارات التطوير (Development Testing)

### اختبار Console Output

افتح Browser Console (F12) وتحقق من الـ logs:

\`\`\`javascript
// عند تغيير الـ Bot يجب أن تشوف:
[BotContext] Active bot changed to: Bot 1

// عند تحديث الـ Bot:
[BotContext] Bot updated: bot-1

// عند حذف الـ Bot:
[BotContext] Bot deleted: bot-1

// عند إنشاء الـ Bot:
[BotContext] Bot created: Bot 3
\`\`\`

---

## 📱 اختبار Responsive

### Mobile (375px):
- [ ] الـ sidebar يختفي تحت الـ navbar
- [ ] زر Menu يظهر
- [ ] Dropdown البروفايل يظهر بشكل صحيح

### Tablet (768px):
- [ ] الـ sidebar مرئي
- [ ] التخطيط responsive
- [ ] الـ dropdowns تظهر في المكان الصحيح

### Desktop (1024px+):
- [ ] الـ sidebar دائماً مرئي
- [ ] لا حاجة لزر Menu
- [ ] layout optimized

---

## 🔄 اختبار الإنشاء الجديد

### إضافة Bot جديد:

#### الخطوات:
1. افتح Sidebar
2. اضغط على "+ New Bot"
3. **تحقق**:
   - ✅ ظهور Bot جديد في القائمة
   - ✅ Bot جديد يكون مختاراً تلقائياً
   - ✅ Dashboard يعرض بيانات Bot جديد فارغة

---

## ✨ اختبار الحالات الحدية

### حالة 1: Bot بدون معارف
\`\`\`javascript
// Bot بدون knowledge
{
  id: 'bot-empty',
  name: 'Empty Bot',
  knowledge: [],
  // ...
}

// متوقع في Dashboard:
- 0 items (في stat)
- Knowledge page تعرض رسالة "No knowledge added"
\`\`\`

### حالة 2: Bot بدون قنوات
\`\`\`javascript
{
  id: 'bot-no-channels',
  name: 'No Channels Bot',
  channels: [],
  // ...
}

// متوقع في Dashboard:
- 0 channels في الـ stat
\`\`\`

### حالة 3: Bot inactive
\`\`\`javascript
{
  id: 'bot-inactive',
  name: 'Inactive Bot',
  status: 'inactive',
  // ...
}

// متوقع في sidebar:
- indicator يكون gray بدلاً من green
\`\`\`

---

## 🚨 اختبارات الأخطاء (Error Testing)

### حذف Bot وهو مختار:
1. اختر Bot 1
2. احذفه (من الـ delete option)
3. **متوقع**:
   - ✅ يتم التبديل تلقائياً لـ Bot 2
   - ✅ Dashboard يحدّث البيانات

---

## 📋 قائمة الاختبار

\`\`\`
[ ] مشكلة RTL/LTR تم إصلاحها
[ ] تبديل الـ Bot يحدّث Dashboard
[ ] Knowledge page تعرض بيانات صحيحة
[ ] تبديل اللغة يعيد التحميل بشكل صحيح
[ ] Responsive تام على جميع الأجهزة
[ ] Console logs صحيحة
[ ] Dropdown Profile يظهر في المكان الصحيح
[ ] Notification dropdown يظهر في المكان الصحيح
[ ] إنشاء Bot جديد يعمل
[ ] حذف Bot يعمل
[ ] حالات الـ Bots الفارغة تعمل بشكل صحيح
\`\`\`

---

## 🐛 troubleshooting

### مشكلة: الـ Bot لا يتغير عند التبديل
**الحل**: تأكد من أن الـ component يستخدم `'use client'`

### مشكلة: Dropdown لا يظهر بشكل صحيح
**الحل**: تحقق من `language` من `useLanguage()`

### مشكلة: البيانات لا تتحدث في Knowledge page
**الحل**: تأكد من استخدام `useMemo` للـ initialItems

---

**آخر تحديث**: 2024-01-22
